{
  "name": "@cleardeed/fetcher-bda-zoning",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "healthcheck": "node -e \"require('./src/index').healthCheck().then(r => { console.log(r ? 'OK' : 'FAIL'); process.exit(r ? 0 : 1); })\""
  },
  "devDependencies": {
    "@turf/turf": "^7.3.5",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
