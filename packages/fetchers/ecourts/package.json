{
  "name": "@cleardeed/fetcher-ecourts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@cleardeed/schema": "workspace:*",
    "tesseract.js": "^5.1.1",
    "zod": "^3.0.0"
  },
  "scripts": {
    "test": "vitest run",
    "healthcheck": "node --input-type=module -e \"import {healthCheck} from './src/index.js'; healthCheck().then(r => { console.log(r ? 'OK' : 'FAIL'); process.exit(r ? 0 : 1); })\""
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}