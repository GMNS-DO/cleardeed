{
  "name": "@cleardeed/fetcher-nominatim",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@cleardeed/schema": "workspace:*",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  },
  "scripts": {
    "test": "vitest run",
    "healthcheck": "node -e \"require('./src/index').healthCheck().then(r => { console.log(r ? 'OK' : 'FAIL'); process.exit(r ? 0 : 1); })\""
  }
}