# Wave 1 Raw Collection Run

Date: 2026-05-26

Command:

```bash
node pid/collectors/wave1.mjs --date=2026-05-26 --max-pdfs=3
```

## Result Summary

| Source | Raw artifacts | Parsed/extracted files | Result |
|---|---:|---:|---|
| Odisha Revenue land acquisition | 4 | 1 | listing page saved, 20 rows parsed, first 3 linked PDFs saved |
| Odisha Revenue SIA 4(1) | 4 | 1 | listing page saved, 20 rows parsed, first 3 linked PDFs saved |
| ORERA/RERA Odisha | 3 | 3 | app shell pages saved; only navigation links visible from static HTML |
| DRT Cuttack cause lists | 2 | 2 | 2 cause-list HTML pages saved; 139 total case rows parsed |
| Bhunaksha/WFS | 1 | 1 | live WFS JSON saved; 41 features returned for Mendhasala/Bhubaneswar sample bbox |

Total:
- 14 raw artifacts
- 9 extracted JSON files
- 14 manifest rows

## Saved Locations

Raw root:

```text
pid/data/raw/
```

Run summary:

```text
pid/data/raw/_runs/2026-05-26/extracted/
```

Source manifests:

```text
pid/data/raw/odisha_revenue_land_acquisition/2026-05-26/manifest.jsonl
pid/data/raw/odisha_revenue_sia_4_1/2026-05-26/manifest.jsonl
pid/data/raw/orera/2026-05-26/manifest.jsonl
pid/data/raw/drt_cuttack_cause_lists/2026-05-26/manifest.jsonl
pid/data/raw/bhunaksha_wfs/2026-05-26/manifest.jsonl
```

## What Worked

- Revenue/SIA listing pages are reliably downloadable and parse into title/date/letter/PDF URL rows.
- Linked Revenue/SIA PDFs download cleanly with artifact hashes.
- DRT seed cause-list URLs download cleanly and parse into case number, applicant, respondent, and raw row text.
- Bhunaksha WFS live sample returns cadastral features with plot/village/admin fields.
- Manifest and hashing discipline works across HTML, PDF, and JSON artifacts.

## What Needs Next

- ORERA static routes return the same SPA shell; project/promoter extraction needs API discovery or browser network capture.
- DRT cause-list discovery still depends on known URLs. Need a date/forum discovery collector.
- Revenue/SIA collector currently saves only the first N PDFs per page. Next step is pagination/all-visible archive with throttling.
- PDF text/OCR extraction is still separate from this raw-save step.
- Bhunaksha should expand from one bbox sample to configured Khordha village/layer grids.

## Verification

```bash
node --check pid/lib/raw-archive.mjs
node --check pid/collectors/wave1.mjs
```

Extracted JSON files under `pid/data/raw/**/extracted/*.json` were parsed successfully.

